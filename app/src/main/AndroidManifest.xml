<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tanmay.rentbaazvehicleadministration">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Entity.ToRent.ToRentActivity" />
        <activity android:name=".Entity.AccountCreation.VerifyAccountActivity" />
        <activity android:name=".Entity.Register.RegisterActivity" />
        <activity android:name=".Entity.AccountCreation.CreateAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Entity.Home.HomeActivity"
            android:label="@string/title_activity_home_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Entity.AddVehicles.AddVehicleActivity" />
        <activity android:name=".Entity.OnRent.OnRentActivity"></activity>
    </application>

</manifest>